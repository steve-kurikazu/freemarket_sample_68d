%h2 購入内容の確認
%ul
  %li
    = image_tag @image.photo.url
  %li
    = @item.name
    %br
    = "¥#{@item.price} (税込)"
    %br
    - if @item.delivery_fee == "送料込み（出品者負担)"
      = "送料込み"
    - else
      = "着払い"
%br
%ul
  %li 支払い金額
  %li 
    = "¥#{@item.price}"
%br
%ul
%h3 支払い方法
- if @default_cards_information.nil?
  %li
    %br
    = @card_error_message
- else
  - @default_cards_information.each_with_index do |card_information, i|
    %li
      %br
      = "カード: #{i + 1}"
      %br
      = "**** **** **** #{card_information.last4}"
      %br
      - exp_month = card_information.exp_month.to_s
      - exp_year = card_information.exp_year.to_s.slice(2,3)
      = exp_month + " / " + exp_year
%br

%h3 配送先
- if @sending.nil?
  %br
  = @sending_error_message
- else
  %br
  = @sending.postal_code.to_s
  %br
  = @prefecture.name + @sending.city + @sending.address + @sending.building_name
  %br
  = @sending.first_name + @sending.last_name
%br
%br
= form_with url: pay_orders_path, local: true do
  %button 購入する

%p　アプリをお持ちでない方は以下よりインストールしてご利用いただけます

.order__contents__btn
  = link_to "#", class: "aBtn" do
    = image_tag '/images/badge/app-store-badge.svg'
  = link_to "#", class: "gBtn" do
    = image_tag '/images/badge/google-play-badge.svg'